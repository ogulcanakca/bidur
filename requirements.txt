# Core dependencies (no UI framework)
openai-agents>=0.6.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# MCP Server dependencies
mcp>=1.0.0
starlette>=0.27.0
uvicorn>=0.24.0
sse-starlette>=1.6.0
httpx>=0.25.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
